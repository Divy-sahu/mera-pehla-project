<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Signup Success</title>
  <style>
    /* Static message */
    .message {
      font-size: 20px;
      font-weight: bold;
      color: black;
    }

    /* Dots container */
    .dots {
      font-size: 24px;
      font-weight: bold;
      color: black;
      display: inline-block;
    }

    /* Animation for each dot */
    .dot {
      opacity: 0;
      display: inline-block;
      animation: blink 1s infinite alternate;
    }

    /* Blink animation for each dot */
    @keyframes blink {
      50% {
        opacity: 1;
      }
    }

    /* Delay for each dot (to make them blink one by one) */
    .dot:nth-child(1) {
      animation-delay: 0s;
    }
    .dot:nth-child(2) {
      animation-delay: 0.3s;
    }
    .dot:nth-child(3) {
      animation-delay: 0.6s;
    }
    .dot:nth-child(4) {
      animation-delay: 0.9s;
    }
    .dot:nth-child(5) {
      animation-delay: 1.2s;
    }
  </style>
  <script>
    // Redirect after a few seconds (optional, you can change delay)
    setTimeout(function() {
      window.location.href = "<%= redirectUrl %>";  // Redirect to Gmail inbox or provided URL
    }, 5000);  // 5 seconds delay before redirecting, adjust as needed
  </script>
</head>
<body>
  <h1>Signup Successful</h1>
  <%= message %>
  <div class="dots">
    <span class="dot">.</span>
    <span class="dot">.</span>
    <span class="dot">.</span>
    <span class="dot">.</span>
    <span class="dot">.</span>
  </div>
</body>
</html>
